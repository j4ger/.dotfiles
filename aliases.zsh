# Navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."

alias dev="cd ~/dev"

# Utilities
alias g="git"
alias grep="grep --color=auto"

alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"

alias chmox="chmod +x"

alias push="git push"

alias plz="sudo"

alias apu="sudo apt update"
alias api="sudo apt install"
